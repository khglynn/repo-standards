# Brief: `bin/audit` Phase 2 + the weekly digest prompt (overnight 2026-09-14)

Read `claude-plans/dependabot-verdicts/GROUNDING.md` and `PLAN.md` first, then `README.md`, `BUILD-LOG.md` (newest section first), `bin/audit`, `bin/lib/`, and GitHub issue khglynn/repo-standards#1 (`gh issue view 1 -R khglynn/repo-standards`) — that issue is the spec, written by the eachie session after a Codex plan review. You are building for Kevin HG, a product person who reads the digest in Slack; he asked for "an automated check so nothing drifts" and "don't break the bank".

## Deliverables (all committed straight to `main` of this repo, small scoped commits, no `git add -A`)
1. **`bin/audit` gains four things** (keep it read-only, keep the existing table and `--json`; add `--digest` which prints a compact plain-English Markdown block for Slack, under 120 words for the headline plus one line per repo that has drift or open Dependabot PRs):
   a. `approve` column: `can_approve_pull_request_reviews` from `GET repos/{r}/actions/permissions/workflow`. An enrolled repo (has the stub) with it `false` is DRIFT: "the workflow's approvals will be refused" (see BUILD-LOG 2026-09-14 00:00 for why).
   b. Actions minutes this billing month per repo, **labelled an estimate**: sum `run_duration_ms` from `GET repos/{r}/actions/runs/{id}/timing` over runs created since the 1st (paginate `GET repos/{r}/actions/runs?created=>=YYYY-MM-01` fully; cap at 300 runs per repo and say when the cap hit). Public repos are free: show the minutes but mark them "public, free". Print the private-repo total against 3,000 and the projected exhaustion date at the current daily rate (usage / days elapsed × days in month). Do not call the billing endpoints (they need scopes this token lacks; say so in a comment).
   c. Jobs without `timeout-minutes`: read every workflow file under `.github/workflows/` (contents API, then parse YAML with PyYAML — it is on this machine; if unavailable, a careful regex on `^\s{2,4}[A-Za-z0-9_-]+:\s*$` job keys is acceptable, say which you used) and list `<workflow>:<job>` pairs missing it. Report as WARNING, not drift.
   d. Double-trigger warning: a workflow with both `push` (to non-default branches, or all branches) and `pull_request`. WARNING, not drift, because it can be intentional.
2. **`routines/weekly-digest.md`**: the prompt for a scheduled Claude Code routine ("Dependabot weekly digest", Monday 08:00 America/Chicago, repo khglynn/repo-standards, connectors Slack + the GitHub connector, model Opus 5). The prompt must: run `bin/audit --digest` if `gh auth status` succeeds in the cloud session, otherwise fall back to the GitHub connector to list open Dependabot PRs per enrolled repo and say plainly that drift checks were skipped; post ONE message to #dependabot (channel id C0C1114321Z) in this shape — a one-line headline (repos enrolled / open Dependabot PRs / oldest / minutes used of 3,000 with the projected run-out date), then one line per repo that has open Dependabot PRs or drift, then "To act:" with the single most useful reply; plain English, no acronyms, no file paths, under 150 words; never wrap anything in angle brackets. Include, in the file, the exact form values so the routine can be created by hand (the form recipe is in BUILD-LOG 2026-09-13 22:44).
3. **Tests in CI**: extend `.github/workflows/ci.yml` / `bin/lib/check-*.sh` with fixture tests for (c) and (d) — a workflow YAML with and without `timeout-minutes`, with and without the double trigger — so the parser cannot silently regress. `actionlint` and `shellcheck` must stay clean.
4. **README**: a short "What the weekly digest tells you" section in plain language, and one line per new audit column. Keep Kevin's mental-model section untouched.
5. **BUILD-LOG.md**: append a dated section as you go (bank early), including anything you could not verify.

## Rules
- Do NOT edit `.github/workflows/dependabot-automerge.yml` tonight (Codex is reviewing it in parallel). Do not touch `templates/caller-stub.yml`.
- `bin/audit` is read-only: no PATCH/PUT/POST anywhere.
- Verify against reality: run `bin/audit --digest` for real against khglynn (it takes ~2 min for 38 repos) and paste the output into BUILD-LOG. Check one repo's minutes estimate by hand against `gh run list` before trusting the column.
- Commit messages: scope-prefixed, one concern each, ending `Co-Authored-By: <your model name> <noreply@anthropic.com>`. Push after each commit.
- Return: what changed (files), the real `--digest` output, the fixture results, and what is unverified.
