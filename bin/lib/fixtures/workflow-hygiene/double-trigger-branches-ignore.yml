# "everything except gh-pages" still covers ordinary feature branches. Expect a finding.
name: CI
on:
  push:
    branches-ignore: [gh-pages]
  pull_request:
jobs:
  checks:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo hi
