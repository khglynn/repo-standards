# Every runnable job carries a time limit. Expect: no findings at all.
name: CI
on:
  pull_request:
jobs:
  checks:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - run: echo hello
  slow:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - run: echo world
